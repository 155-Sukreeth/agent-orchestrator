FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy agents package
COPY agents/ ./agents/

# Command to run agents API
CMD ["uvicorn", "agents.main:app", "--host", "0.0.0.0", "--port", "8001"]
